import {createGlobalStyle} from "styled-components";

const Utils = createGlobalStyle`
  .relative {
    position: relative;
  }

  .center {
    text-align: center;
  }

  .uppercase {
    text-transform: uppercase;
  }
`;

export default Utils;
